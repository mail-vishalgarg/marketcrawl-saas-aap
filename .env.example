# Copy this file to .env and fill in your values
# .env is gitignored — never commit real secrets

# Example keys (add your own as needed)
# API_KEY=your_api_key_here
# DATABASE_URL=your_database_url_here
